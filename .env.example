# คัดลอกไฟล์นี้เป็น .env แล้วกรอกค่าจริง (อย่า commit .env ขึ้น git)

# คีย์ลับสำหรับเซ็น JWT — สุ่มให้ยาวและเดายาก เช่น `openssl rand -hex 32`
JWT_SECRET=

# OpenAI API key สำหรับฟีเจอร์ AI (ตรวจงาน/ประเมินแผน/สร้างภาพ)
OPENAI_API_KEY=
